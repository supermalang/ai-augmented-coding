# Code map (generated — do not edit by hand)

> Machine-generated router index of the codebase — the fast answer to *"where does this
> live, and what depends on what?"* **read this before grepping the tree.** Regenerate with
> `/code-map` (or the `STACK_CODE_MAP_CMD` in `.claude/hooks/stack-profile.sh`). Edges are a
> heuristic (import/require/from grep + alias resolution) — treat them as a strong hint, not
> gospel; verify against the tree when precision matters. For the *semantics* a script can't
> infer (responsibility, public-API entry points), see the hand-curated `## Code map
> (navigation)` table in `docs/ARCHITECTURE.md`.
>
> tree-stamp: 6031c6001589 · groups: 2 · code files: 12

| Area | Key files | Files | Depends on |
|---|---|---|---|
| `.claude/skills` | lib.mjs, archive.mjs, archive.test.mjs | 11 | — |
| `.claude/reporting` | generate-image.mjs | 1 | — |
